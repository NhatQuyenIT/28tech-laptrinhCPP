#include <iostream>
#include <iomanip>
#include <math.h>
using namespace std;

int main(){
	int a,b,c;
	cin >> a >> b
	cin >> c;
	cout << abs(c) << endl;
	cout << fixed << setprecision(2) << sqrt(c) << endl;
	cout << pow(a,b) << endl;
	return 0;
}
